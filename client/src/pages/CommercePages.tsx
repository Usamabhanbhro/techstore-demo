import { useEffect, useMemo, useState, type ChangeEvent, type FormEvent } from "react";
import { ArrowRight, Check, ChevronDown, Heart, Minus, Plus, Search, Truck } from "lucide-react";
import { Link, useLocation, useRoute } from "wouter";
import { collections, findCollection, findJournal, findProduct, journals, money, products, type Product } from "@/lib/catalog";
import { cartLineKey, useCommerce, wishlistProducts, type DemoOrder } from "@/lib/commerce";
import { paymentProviders, paymentRegistry, type PaymentMethod, type PaymentResult } from "@/lib/payment";
import { PageIntro } from "@/components/Storefront";
import { ProductGrid } from "@/components/ProductCard";

const RECENTLY_VIEWED_KEY = "usamabhanbhro-recently-viewed";
const PROMO_KEY = "usamabhanbhro-promo";
const familyOrder = ["iPhone", "Mac", "iPad", "Apple Watch", "AirPods", "Apple Vision Pro", "TV & Home", "Accessories"];
const readSearch = () => typeof window === "undefined" ? "" : new URLSearchParams(window.location.search).get("q") ?? "";
const readPromo = () => typeof window === "undefined" ? "" : window.sessionStorage.getItem(PROMO_KEY) ?? "";
const collectionHref = (category: string) => `/collections/${collections.find((collection) => collection.category === category)?.slug ?? category.toLowerCase().replaceAll(" & ", "-").replaceAll(" ", "-")}`;
const availabilityCopy = (product: Product) => product.availability === "out-of-stock" ? "Currently unavailable" : product.availability === "low-stock" ? "Low stock — order soon" : "Available now";

function recordRecentlyViewed(productId: string) {
  if (typeof window === "undefined") return;
  const prior = (window.localStorage.getItem(RECENTLY_VIEWED_KEY) ?? "").split(",").filter(Boolean);
  window.localStorage.setItem(RECENTLY_VIEWED_KEY, [productId, ...prior.filter((id) => id !== productId)].slice(0, 6).join(","));
}
function readRecentlyViewed(excludeProductId: string) {
  if (typeof window === "undefined") return [];
  const raw = window.localStorage.getItem(RECENTLY_VIEWED_KEY) ?? "";
  return raw.split(",").filter(Boolean).filter((id) => id !== excludeProductId).map((id) => products.find((item) => item.id === id)).filter(Boolean) as Product[];
}
function productFromId(id: string) { return products.find((item) => item.id === id); }

export function ShopPage({ collectionSlug }: { collectionSlug?: string }) {
  const collection = collectionSlug ? findCollection(collectionSlug) : undefined;
  const [, navigate] = useLocation();
  const categories = ["All", ...familyOrder.filter((item) => products.some((product) => product.category === item))];
  const [category, setCategory] = useState(collection?.category ?? "All");
  const [availability, setAvailability] = useState<"all" | "in-stock">("all");
  const [sort, setSort] = useState("featured");
  const [price, setPrice] = useState("all");
  useEffect(() => setCategory(collection?.category ?? "All"), [collection?.slug, collection?.category]);
  const items = useMemo(() => {
    let result = collection ? products.filter((product) => product.category === collection.category || product.collection === collection.name || product.collections.includes(collection.slug)) : products;
    if (category !== "All") result = result.filter((product) => product.category === category);
    if (availability === "in-stock") result = result.filter((product) => product.availability !== "out-of-stock");
    if (price === "under-500") result = result.filter((product) => product.price < 500);
    if (price === "500-1000") result = result.filter((product) => product.price >= 500 && product.price <= 1000);
    if (price === "over-1000") result = result.filter((product) => product.price > 1000);
    return [...result].sort((a, b) => {
      if (sort === "price-low") return a.price - b.price;
      if (sort === "price-high") return b.price - a.price;
      if (sort === "new") return Number(b.flags.includes("new-arrival")) - Number(a.flags.includes("new-arrival"));
      if (sort === "name") return a.name.localeCompare(b.name);
      return Number(b.flags.includes("featured") || b.flags.includes("best-seller")) - Number(a.flags.includes("featured") || a.flags.includes("best-seller"));
    });
  }, [availability, category, collection, price, sort]);
  const reset = () => { setCategory(collection?.category ?? "All"); setAvailability("all"); setPrice("all"); setSort("featured"); };
  return <><PageIntro eyebrow={collection?.eyebrow ?? "Apple Store"} title={collection?.name ?? "Shop Apple"} body={collection?.description ?? "Explore the latest Apple products, accessories, and services. Shop online with free delivery and Apple Store support."} />
    {collection && <div className="collection-banner"><img src={collection.image} alt={`${collection.name} collection`} /><div><p className="eyebrow eyebrow--light">{collection.name}</p><h2>Designed to work<br />beautifully together.</h2><button className="button button--light" onClick={() => navigate("/compare")}>Compare models</button></div></div>}
    <section className="listing-section"><div className="filter-bar"><div className="filter-buttons" aria-label="Product category">{categories.map((item) => <button className={category === item ? "is-active" : ""} onClick={() => setCategory(item)} key={item}>{item}</button>)}</div><div className="filter-selects"><label className="sort-control">Availability <select value={availability} onChange={(event) => setAvailability(event.target.value as "all" | "in-stock")}><option value="all">All products</option><option value="in-stock">Available now</option></select></label><label className="sort-control">Price <select value={price} onChange={(event) => setPrice(event.target.value)}><option value="all">Any price</option><option value="under-500">Under $500</option><option value="500-1000">$500–$1,000</option><option value="over-1000">Over $1,000</option></select></label><label className="sort-control">Sort <select value={sort} onChange={(event) => setSort(event.target.value)}><option value="featured">Featured</option><option value="new">New arrivals</option><option value="name">Name</option><option value="price-low">Price: low to high</option><option value="price-high">Price: high to low</option></select></label></div></div><p className="result-count">{items.length} {items.length === 1 ? "product" : "products"} · Free delivery available</p>{items.length ? <ProductGrid items={items} /> : <div className="empty-state"><h2>No products match those filters.</h2><p>Try another category or clear the price and availability filters.</p><button className="button button--outline" onClick={reset}>Reset filters</button></div>}</section></>;
}

export function CollectionsPage() { return <><PageIntro eyebrow="Apple Store" title="Shop by product family" body="Find the product that fits your work, your creativity, your health, and your home." /><section className="collection-index">{collections.map((collection) => <Link className="collection-index__item" href={`/collections/${collection.slug}`} key={collection.slug}><img src={collection.image} alt={collection.name} loading="lazy" /><div><p className="eyebrow">{collection.eyebrow}</p><h2>{collection.name}</h2><span className="text-link">Shop {collection.name} <ArrowRight size={13} /></span></div></Link>)}</section></>; }

export function ProductPage() {
  const [, params] = useRoute<{ slug: string }>("/products/:slug");
  const product = findProduct(params?.slug);
  const { addToCart, toggleWishlist, wishlist } = useCommerce();
  const [selectedVariant, setSelectedVariant] = useState(product?.variants[0]?.value ?? "");
  const [quantity, setQuantity] = useState(1);
  const [added, setAdded] = useState(false);
  const [recentlyViewed, setRecentlyViewed] = useState<Product[]>([]);
  useEffect(() => { if (!product) return; recordRecentlyViewed(product.id); setRecentlyViewed(readRecentlyViewed(product.id)); setSelectedVariant(product.variants[0]?.value ?? ""); setAdded(false); }, [product?.id]);
  if (!product) return <NotFoundPage />;
  const selected = product.variants.find((option) => option.value === selectedVariant) ?? product.variants[0];
  const price = selected?.price ?? product.price;
  const related = product.relatedProductIds.map(productFromId).filter(Boolean) as Product[];
  const accessories = product.accessories.map(productFromId).filter(Boolean) as Product[];
  const saved = wishlist.includes(product.id);
  const canBuy = product.availability !== "out-of-stock" && Boolean(selected?.available);
  return <section className="product-detail"><div className="breadcrumbs"><Link href="/shop">Shop</Link><span>/</span><Link href={collectionHref(product.category)}>{product.category}</Link><span>/</span><span>{product.name}</span></div><div className="product-detail__grid"><div className="product-gallery">{product.images.map((src, index) => <img src={src} alt={`${product.name} view ${index + 1}`} key={`${src}-${index}`} />)}</div><div className="product-detail__info"><p className="eyebrow">{product.category} · {product.collection}</p><h1>{product.name}</h1><p className="product-subtitle">{product.subtitle}</p><div className="product-price-row"><strong className="product-price">{money(price)}</strong>{product.compareAtPrice && <del>{money(product.compareAtPrice)}</del>}</div><p className="product-description">{product.description}</p><div className={`availability-panel availability-panel--${product.availability}`}><strong>{availabilityCopy(product)}</strong><span><Truck size={15} /> Get it by {new Intl.DateTimeFormat("en-US", { weekday: "long", month: "short", day: "numeric" }).format(new Date(Date.now() + 4 * 86400000))} with free delivery.</span></div><div className="variant-selector"><span>{product.variants[0]?.label ?? "Configuration"}</span><div>{product.variants.map((option) => <button className={selectedVariant === option.value ? "is-selected" : ""} disabled={!option.available} onClick={() => setSelectedVariant(option.value)} key={option.value}>{option.value}{!option.available && " · unavailable"}</button>)}</div></div><div className="quantity-row"><span>Quantity</span><div><button aria-label="Decrease quantity" onClick={() => setQuantity(Math.max(1, quantity - 1))}><Minus size={15} /></button><span>{quantity}</span><button aria-label="Increase quantity" onClick={() => setQuantity(Math.min(10, quantity + 1))}><Plus size={15} /></button></div></div><div className="product-actions"><button className="button button--dark" disabled={!canBuy} onClick={() => { addToCart(product, quantity, selectedVariant); setAdded(true); }}>{canBuy ? (added ? <><Check size={16} /> Added to bag</> : "Add to bag") : "Currently unavailable"}</button><button className="button button--outline" onClick={() => toggleWishlist(product.id)}><Heart size={16} fill={saved ? "currentColor" : "none"} />{saved ? "Saved to wishlist" : "Save to wishlist"}</button></div><div className="detail-notes">{product.details.map((detail) => <p key={detail}><Check size={14} /><span>{detail}</span></p>)}</div><details className="product-accordion" open><summary>Product details <ChevronDown size={16} /></summary><div className="spec-grid">{product.specifications.map((spec) => <div key={spec.label}><span>{spec.label}</span><strong>{spec.value}</strong></div>)}</div></details><details className="product-accordion"><summary>Compatibility <ChevronDown size={16} /></summary><p>{product.compatibility.length ? `Designed to work with ${product.compatibility.join(", ")}.` : "See the product details above for supported configurations."}</p></details><details className="product-accordion"><summary>What’s included <ChevronDown size={16} /></summary><ul>{product.whatsIncluded.map((item) => <li key={item}>{item}</li>)}</ul></details><details className="product-accordion"><summary>Delivery and pickup <ChevronDown size={16} /></summary><p>Choose free delivery or Apple Store pickup at checkout. This local demo does not process real orders.</p></details><p className="product-care"><strong>Care.</strong> {product.care}</p></div></div>{accessories.length > 0 && <section className="related-section"><div className="section-heading"><div><p className="eyebrow">Compatible accessories</p><h2>Complete your setup.</h2></div><Link className="text-link" href="/collections/accessories">Shop accessories <ArrowRight size={13} /></Link></div><p className="section-supporting">Accessories selected for {product.name}.</p><ProductGrid items={accessories} /></section>}{related.length > 0 && <section className="related-section related-section--secondary"><div className="section-heading"><div><p className="eyebrow">You might also like</p><h2>Keep exploring.</h2></div></div><ProductGrid items={related} /></section>}{recentlyViewed.length > 0 && <section className="related-section related-section--secondary"><div className="section-heading"><div><p className="eyebrow">Recently viewed</p><h2>Pick up where you left off.</h2></div></div><ProductGrid items={recentlyViewed} /></section>}</section>;
}

export function ComparePage() {
  const families = ["iPhone", "Mac", "iPad", "Apple Watch", "AirPods"];
  const [family, setFamily] = useState("iPhone");
  const items = products.filter((product) => product.category === family).slice(0, 4);
  const labels = Array.from(new Set(items.flatMap((item) => item.specifications.map((spec) => spec.label))));
  const rows = labels.map((label) => ({ label, values: items.map((item) => item.specifications.find((spec) => spec.label === label)?.value ?? "—") })).filter((row) => row.values.some((value) => value !== "—"));
  return <section className="compare-page"><PageIntro eyebrow="Apple Store" title="Compare products" body="Choose a family, compare the essentials, and find the Apple product that fits you best." /><div className="compare-tabs" role="tablist">{families.map((item) => <button role="tab" aria-selected={family === item} className={family === item ? "is-active" : ""} key={item} onClick={() => setFamily(item)}>{item}</button>)}</div>{items.length ? <><div className="compare-table-wrap"><table className="compare-table"><thead><tr><th>Feature</th>{items.map((item) => <th key={item.id}><img src={item.images[0]} alt="" /><Link href={`/products/${item.slug}`}>{item.name}</Link><strong>{money(item.price)}</strong><span className="availability availability--in-stock">Available now</span></th>)}</tr></thead><tbody><tr><th>Best for</th>{items.map((item) => <td key={item.id}>{item.subtitle}</td>)}</tr>{rows.map((row) => <tr key={row.label}><th>{row.label}</th>{row.values.map((value, index) => <td key={`${row.label}-${items[index].id}`}>{value}</td>)}</tr>)}</tbody></table></div><Link className="button button--dark" href={`/products/${items[0].slug}`}>Shop {family}</Link></> : <div className="empty-state"><h2>No products to compare.</h2><p>Choose another product family.</p></div>}</section>;
}

export function SearchPage() {
  const [query, setQuery] = useState(readSearch);
  const [category, setCategory] = useState("All");
  const categories = ["All", ...familyOrder.filter((item) => products.some((product) => product.category === item))];
  const results = products.filter((product) => {
    const searchable = [product.name, product.subtitle, product.description, product.category, product.family, product.collection, ...product.tags, ...product.details, ...product.features, ...product.compatibility, ...product.specifications.map((spec) => `${spec.label} ${spec.value}`), ...product.variants.map((variant) => variant.value)].join(" ").toLowerCase();
    return (category === "All" || product.category === category) && searchable.includes(query.trim().toLowerCase());
  });
  return <><PageIntro eyebrow="Apple Store" title="Search" body="Search products, accessories, and support topics by name, family, compatibility, or feature." /><section className="search-section"><div className="search-box"><Search size={20} aria-hidden="true" /><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search iPhone, Mac, AirPods, or accessories" aria-label="Search products" />{query && <button onClick={() => setQuery("")} aria-label="Clear search">Clear</button>}</div><div className="filter-buttons" aria-label="Search category">{categories.map((item) => <button className={category === item ? "is-active" : ""} onClick={() => setCategory(item)} key={item}>{item}</button>)}</div><p className="result-count">{query ? `${results.length} results for “${query}”` : `Browse all ${products.length} products`}</p>{results.length ? <ProductGrid items={results} /> : <div className="empty-state"><h2>No products found.</h2><p>Try iPhone, Mac, iPad, Watch, AirPods, or accessories.</p><button className="button button--outline" onClick={() => { setQuery(""); setCategory("All"); }}>Clear search</button></div>}</section></>;
}

function PromoField({ value, onChange }: { value: string; onChange: (value: string) => void }) { const invalid = Boolean(value && value.trim().toUpperCase() !== "APPLE10"); return <div className="promo-field"><label htmlFor="promo-code">Promo code</label><div><input id="promo-code" value={value} onChange={(event) => onChange(event.target.value)} placeholder="Try APPLE10" aria-invalid={invalid} /><span className={invalid ? "promo-error" : "promo-success"}>{invalid ? "Enter a valid code" : value ? "10% demo discount applied" : ""}</span></div></div>; }
function Summary({ subtotal, shipping, discount, promoCode, onPromoChange }: { subtotal: number; shipping: number; discount: number; promoCode: string; onPromoChange: (value: string) => void }) { return <aside className="order-summary"><p className="eyebrow">Order summary</p><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><PromoField value={promoCode} onChange={onPromoChange} />{discount > 0 && <div><span>Demo discount</span><strong>−{money(discount)}</strong></div>}<div><span>Delivery</span><strong>{shipping ? money(shipping) : "Free"}</strong></div><div className="order-summary__total"><span>Total</span><strong>{money(subtotal + shipping - discount)}</strong></div><Link className="button button--dark" href="/checkout">Continue to checkout</Link><p className="summary-note">This is a local demo. No payment is processed.</p></aside>; }

export function CartPage() { const { cart, subtotal, setQuantity, removeFromCart } = useCommerce(); const [promoCode, setPromoCode] = useState(readPromo); const shipping = subtotal > 0 && subtotal < 50 ? 9 : 0; const discount = promoCode.trim().toUpperCase() === "APPLE10" ? Math.round(subtotal * 0.1) : 0; const updatePromo = (value: string) => { setPromoCode(value); if (typeof window !== "undefined") window.sessionStorage.setItem(PROMO_KEY, value); }; return <section className="cart-page"><PageIntro eyebrow="Apple Store" title="Your bag" body="Review your products before you continue to checkout." />{cart.length ? <div className="cart-layout"><div className="cart-lines">{cart.map((line) => <div className="cart-line" key={cartLineKey(line)}><img src={line.product.images[0]} alt={line.product.name} /><div><p className="eyebrow">{line.product.category}</p><h2>{line.product.name}</h2><p>{line.variant ?? line.product.variants[0]?.value}</p><span className={`availability availability--${line.product.availability}`}>{availabilityCopy(line.product)}</span><button className="plain-link" onClick={() => removeFromCart(line.product.id, line.variant)}>Remove</button></div><div className="cart-line__controls"><div><button aria-label="Decrease quantity" onClick={() => setQuantity(line.product.id, line.quantity - 1, line.variant)}><Minus size={15} /></button><span>{line.quantity}</span><button aria-label="Increase quantity" onClick={() => setQuantity(line.product.id, line.quantity + 1, line.variant)}><Plus size={15} /></button></div><strong>{money(line.product.price * line.quantity)}</strong></div></div>)}</div><Summary subtotal={subtotal} shipping={shipping} discount={discount} promoCode={promoCode} onPromoChange={updatePromo} /></div> : <div className="empty-state"><h2>Your bag is empty.</h2><p>When you add an Apple product, it will appear here.</p><Link className="button button--dark" href="/shop">Shop Apple</Link></div>}</section>; }

export function AccountPage() { const { lastOrder, wishlist } = useCommerce(); return <><PageIntro eyebrow="Apple Store" title="Your account" body="Track your local demo order, saved products, and shopping preferences." /><section className="dashboard-grid"><div className="dashboard-card dashboard-card--dark"><p className="eyebrow eyebrow--light">Demo profile</p><h2>Demo shopper</h2><p>shopper@example.com</p><span>Local account state</span></div><div className="dashboard-card"><p className="eyebrow">Orders</p><h2>{lastOrder ? "1" : "0"}</h2><p>{lastOrder ? `Latest order ${lastOrder.id}` : "No demo orders yet"}</p><Link className="plain-link" href={lastOrder ? "/order-confirmation" : "/shop"}>{lastOrder ? "View order" : "Start shopping"}</Link></div><div className="dashboard-card"><p className="eyebrow">Saved products</p><h2>{wishlist.length}</h2><p>Products saved for later.</p><Link className="plain-link" href="/wishlist">View wishlist</Link></div><div className="dashboard-card dashboard-card--wide"><p className="eyebrow">Support</p><h2>We’re here to help.</h2><p>Get help choosing a product, setting up a device, or managing a local demo order.</p><Link className="plain-link" href="/contact">Contact support</Link></div></section></>; }

export function WishlistPage() { const { wishlist, toggleWishlist } = useCommerce(); const items = wishlistProducts(wishlist); return <><PageIntro eyebrow="Apple Store" title="Saved products" body="Keep products here while you compare and decide." />{items.length ? <><section className="listing-section"><ProductGrid items={items} /></section><div className="wishlist-note"><p>{items.length} saved {items.length === 1 ? "product" : "products"} · changes stay in this browser.</p><button className="plain-link" onClick={() => items.forEach((item) => toggleWishlist(item.id))}>Clear wishlist</button></div></> : <div className="empty-state"><h2>Nothing saved yet.</h2><p>Use the heart on a product card to keep it close.</p><Link className="button button--dark" href="/shop">Shop Apple</Link></div>}</>; }

export function JournalPage() { return <><PageIntro eyebrow="Apple Store guides" title="Find the right product for you." body="Compare devices, build your setup, and get more from the products you already own." /><section className="journal-index">{journals.map((journal) => <Link className="journal-index__item" href={`/journal/${journal.slug}`} key={journal.slug}><img src={journal.image} alt={journal.title} loading="lazy" /><div><p className="eyebrow">{journal.type} · {journal.date}</p><h2>{journal.title}</h2><p>{journal.excerpt}</p><span className="text-link">Read guide <ArrowRight size={13} /></span></div></Link>)}</section></>; }
export function ArticlePage() { const [, params] = useRoute<{ slug: string }>("/journal/:slug"); const article = findJournal(params?.slug); if (!article) return <NotFoundPage />; return <article className="article-page"><div className="article-hero"><img src={article.image} alt={article.title} /><div><p className="eyebrow eyebrow--light">{article.type} · {article.date}</p><h1>{article.title}</h1></div></div><div className="article-body"><p className="article-lede">{article.excerpt}</p><p>Apple products are designed to work together. Start with the product that matters most to you, then add the accessories and services that make your everyday experience easier.</p><p>Whether you are choosing a phone, a Mac, an iPad, or a watch, compare the features that matter to your work and your life. Apple Store specialists can help you find the right fit.</p><blockquote>“The best product is the one that fits the way you live.”</blockquote><p>Explore the collection with the same curiosity. Notice the details, compare the options, and find the product that stays with you.</p><Link className="text-link" href="/shop">Shop Apple <ArrowRight size={13} /></Link></div><div className="article-related"><p className="eyebrow">Keep exploring</p><div>{journals.filter((item) => item.slug !== article.slug).map((item) => <Link href={`/journal/${item.slug}`} key={item.slug}>{item.title} <ArrowRight size={13} /></Link>)}</div></div></article>; }
export function AboutPage() { return <><PageIntro eyebrow="Apple Store" title="Designed to work beautifully together." body="Explore products, services, support, and shopping options in one place." /><section className="manifesto"><div className="manifesto__image"><img src="/manus-storage/1786605681885_2144d048.png" alt="Apple products and accessories arranged in a studio composition" /></div><div className="manifesto__copy"><p className="eyebrow">The Apple experience</p><h2>Powerful products.<br />Personal support.</h2><p>Apple products are designed around privacy, accessibility, performance, and a seamless experience across devices.</p><p>This website is a transparent commerce demonstration: products, cart, wishlist, and checkout are local and no real transaction is processed.</p><Link className="text-link" href="/contact">Get support <ArrowRight size={13} /></Link></div></section></>; }
export function ContactPage() { const [sent, setSent] = useState(false); const submit = (event: FormEvent) => { event.preventDefault(); setSent(true); }; return <><PageIntro eyebrow="Apple Store support" title="We’re here to help." body="Ask about choosing a product, setting up a device, or navigating this local demo." /><section className="contact-layout"><form className="contact-form" onSubmit={submit}><label>Name<input required placeholder="Your name" /></label><label>Email<input required type="email" placeholder="you@example.com" /></label><label>How can we help?<textarea required placeholder="Tell us what you need" rows={6} /></label><button className="button button--dark" type="submit">{sent ? <><Check size={16} /> Message received</> : "Send message"}</button>{sent && <p className="form-success" role="status">Thanks — this demo recorded your message locally. No email was sent.</p>}</form><div className="contact-aside"><p className="eyebrow">Apple Store</p><h2>Online<br />In store<br />Everywhere</h2><p>Get help choosing, buying, setting up, and using Apple products.</p><Link className="plain-link" href="/shop">Continue shopping</Link></div></section></>; }
export function NotFoundPage() { return <section className="not-found"><p className="eyebrow">404 · page not found</p><h1>We can’t find<br />that page.</h1><p>Return to the Apple Store and choose another destination.</p><Link className="button button--dark" href="/shop">Shop Apple</Link></section>; }

function FieldError({ message }: { message?: string }) { return message ? <span className="field-error" role="alert">{message}</span> : null; }

export function CheckoutPage() {
  const { cart, subtotal, clearCart, createOrder } = useCommerce(); const [, navigate] = useLocation();
  const [method, setMethod] = useState<PaymentMethod | "">(""); const [deliveryMethod, setDeliveryMethod] = useState<"standard" | "pickup">("standard"); const [outcome, setOutcome] = useState<"success" | "failure" | "pending" | "cancelled">("success"); const [processing, setProcessing] = useState(false); const [result, setResult] = useState<PaymentResult | null>(null); const [promoCode, setPromoCode] = useState(readPromo); const [errors, setErrors] = useState<Record<string, string>>({});
  const [form, setForm] = useState({ name: "", email: "", address: "", city: "", state: "", zip: "" });
  const shipping = deliveryMethod === "pickup" || subtotal === 0 || subtotal >= 50 ? 0 : 9; const discount = promoCode.trim().toUpperCase() === "APPLE10" ? Math.round(subtotal * 0.1) : 0; const total = subtotal + shipping - discount; const provider = method ? paymentRegistry[method] : undefined;
  const update = (key: keyof typeof form) => (event: ChangeEvent<HTMLInputElement>) => { setForm((current) => ({ ...current, [key]: event.target.value })); setErrors((current) => ({ ...current, [key]: "" })); };
  const updatePromo = (value: string) => { setPromoCode(value); if (typeof window !== "undefined") window.sessionStorage.setItem(PROMO_KEY, value); };
  if (!cart.length && !result) return <section className="checkout-page commerce-state"><div className="commerce-state__stamp">APPLE STORE DEMO · NO MONEY MOVED</div><PageIntro eyebrow="Checkout" title="Your bag is empty" body="Return to the shop to start your Apple Store demo journey." /><Link className="button button--dark" href="/shop">Browse Apple Store</Link></section>;
  const validate = () => { const next: Record<string, string> = {}; if (!form.name.trim()) next.name = "Enter your full name."; if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email)) next.email = "Enter a valid email address."; if (!form.address.trim()) next.address = "Enter a delivery address."; if (!form.city.trim()) next.city = "Enter your city."; if (!form.state.trim()) next.state = "Enter your state."; if (!/^\d{5}(-\d{4})?$/.test(form.zip.trim())) next.zip = "Enter a valid ZIP code."; if (!method) next.payment = "Choose a demo payment method."; if (promoCode && promoCode.trim().toUpperCase() !== "APPLE10") next.promo = "Enter APPLE10 or clear this field."; setErrors(next); return Object.keys(next).length === 0; };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!validate() || !provider) return; setProcessing(true); setResult(null); const orderId = `DEMO-${Date.now()}`; const response = await provider.initialize({ amount: total, orderId, demoOutcome: outcome }); setResult(response); setProcessing(false); if (response.status === "successful" || response.status === "pending") { const order: DemoOrder = { id: `APPLE-${Date.now().toString().slice(-8)}`, date: new Date().toISOString(), items: cart, configurations: Object.fromEntries(cart.map((line) => [line.product.id, line.variant])), subtotal, discount, shipping, delivery: shipping, total, customer: { name: form.name, email: form.email }, deliveryAddress: { address: form.address, city: form.city, state: form.state, zip: form.zip }, status: response.status === "pending" ? "pending" : "confirmed", paymentMethod: provider.name, paymentStatus: response.status, referenceId: response.referenceId }; createOrder(order); clearCart(); navigate("/order-confirmation"); } };
  return <section className="checkout-page commerce-state"><div className="commerce-state__stamp">APPLE STORE DEMO · NO MONEY MOVED</div><PageIntro eyebrow="Checkout" title="Review your order." body="This checkout simulates provider states locally. Never enter a real card number or financial credential." /><form className="checkout-layout" onSubmit={submit} noValidate><div className="checkout-form"><fieldset><legend>Contact</legend><label>Full name<input value={form.name} onChange={update("name")} placeholder="Amina Khan" aria-invalid={Boolean(errors.name)} /><FieldError message={errors.name} /></label><label>Email address<input type="email" value={form.email} onChange={update("email")} placeholder="you@example.com" aria-invalid={Boolean(errors.email)} /><FieldError message={errors.email} /></label></fieldset><fieldset><legend>Delivery address</legend><label>Address<input value={form.address} onChange={update("address")} placeholder="1 Apple Park Way" aria-invalid={Boolean(errors.address)} /><FieldError message={errors.address} /></label><div className="checkout-grid"><label>City<input value={form.city} onChange={update("city")} placeholder="Cupertino" aria-invalid={Boolean(errors.city)} /><FieldError message={errors.city} /></label><label>State<input value={form.state} onChange={update("state")} placeholder="CA" aria-invalid={Boolean(errors.state)} /><FieldError message={errors.state} /></label><label>ZIP code<input value={form.zip} onChange={update("zip")} placeholder="95014" inputMode="numeric" aria-invalid={Boolean(errors.zip)} /><FieldError message={errors.zip} /></label></div><div className="delivery-choice"><span>Delivery method</span><label><input type="radio" checked={deliveryMethod === "standard"} onChange={() => setDeliveryMethod("standard")} name="delivery" /> Standard delivery · {shipping ? money(shipping) : "Free"}</label><label><input type="radio" checked={deliveryMethod === "pickup"} onChange={() => setDeliveryMethod("pickup")} name="delivery" /> Apple Store pickup · Free</label></div></fieldset><fieldset><legend>Payment method</legend><p className="demo-form-note">Provider connectors are simulated; no real API or credential is used.</p><div className="payment-options">{paymentProviders.map((item) => <label className={`payment-option ${method === item.id ? "is-selected" : ""}`} key={item.id}><input type="radio" name="payment" checked={method === item.id} onChange={() => { setMethod(item.id); setErrors((current) => ({ ...current, payment: "" })); }} /><span><strong>{item.name}</strong><small>{item.description}</small></span><span className="payment-option__mark">{method === item.id ? <Check size={15} /> : ""}</span></label>)}</div><FieldError message={errors.payment} /><label className="demo-outcome">Demo outcome<select value={outcome} onChange={(event) => setOutcome(event.target.value as typeof outcome)}><option value="success">Successful</option><option value="pending">Pending</option><option value="failure">Failed</option><option value="cancelled">Cancelled</option></select></label></fieldset>{result && <div className={`payment-result payment-result--${result.status}`} role="alert"><strong>{result.status === "failed" ? "Payment needs attention" : `Demo payment ${result.status}`}</strong><p>{result.message}</p><small>Reference: {result.referenceId}</small>{result.status === "failed" && <div className="payment-result__actions"><button type="button" className="plain-link" onClick={() => setResult(null)}>Retry</button><button type="button" className="plain-link" onClick={() => { setResult(null); setMethod(""); }}>Choose another method</button></div>}</div>}</div><aside className="order-summary"><p className="eyebrow eyebrow--rust">Order summary</p>{cart.map((line) => <div key={cartLineKey(line)}><span>{line.product.name} × {line.quantity}</span><strong>{money(line.product.price * line.quantity)}</strong></div>)}<PromoField value={promoCode} onChange={updatePromo} />{errors.promo && <FieldError message={errors.promo} />}<div><span>Subtotal</span><strong>{money(subtotal)}</strong></div>{discount > 0 && <div><span>Demo discount</span><strong>−{money(discount)}</strong></div>}<div><span>Delivery</span><strong>{shipping ? money(shipping) : "Free"}</strong></div><div className="order-summary__total"><span>Total</span><strong>{money(total)}</strong></div><button className="button button--dark" disabled={processing} type="submit">{processing ? "Processing demo…" : provider ? `Place demo order · ${provider.name}` : "Choose payment method"}</button><p className="summary-note">By continuing, you acknowledge this is a non-financial showcase only.</p></aside></form></section>;
}

export function ConfirmationPage() { const { lastOrder } = useCommerce(); if (!lastOrder) return <NotFoundPage />; return <section className="confirmation-page commerce-state"><div className="commerce-state__stamp commerce-state__stamp--rust">ORDER CONFIRMATION · LOCAL ONLY</div><div className="confirmation-mark"><Check size={26} /></div><p className="eyebrow eyebrow--rust">Thank you</p><h1>Your order is<br />{lastOrder.status === "pending" ? "being reviewed." : "confirmed."}</h1><p className="confirmation-lede">Your demo order has been created locally. No payment was processed and no fulfillment has been initiated.</p><div className="confirmation-card"><div><span>Order</span><strong>{lastOrder.id}</strong></div><div><span>Date</span><strong>{new Intl.DateTimeFormat("en-US", { dateStyle: "medium" }).format(new Date(lastOrder.date))}</strong></div><div><span>Payment</span><strong>{lastOrder.paymentMethod} · {lastOrder.paymentStatus}</strong></div><div><span>Reference</span><strong>{lastOrder.referenceId}</strong></div><div><span>Total</span><strong>{money(lastOrder.total)}</strong></div><div><span>Deliver to</span><strong>{lastOrder.customer.name}<br />{lastOrder.deliveryAddress.city}, {lastOrder.deliveryAddress.state}</strong></div></div><div className="confirmation-items"><p className="eyebrow eyebrow--rust">Items</p>{lastOrder.items.map((line) => <div key={cartLineKey(line)}><span>{line.product.name} · {line.variant} × {line.quantity}</span><strong>{money(line.product.price * line.quantity)}</strong></div>)}</div><div className="confirmation-actions"><Link className="button button--dark" href="/shop">Continue shopping</Link><Link className="button button--outline" href="/account">View account</Link></div></section>; }
