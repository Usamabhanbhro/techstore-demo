/* Oroton reference clone: single-route presentation with image-led editorial hierarchy and restrained interaction. */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}

